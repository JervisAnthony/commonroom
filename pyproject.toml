[project]
name = "commonroom-workspace"
version = "0.0.0"
description = "Python workspace root for the Commonroom ecosystem."
requires-python = ">=3.13"
dependencies = []

[tool.uv]
package = false
required-version = ">=0.12,<0.13"

[tool.uv.workspace]
members = ["apps/*/api"]

