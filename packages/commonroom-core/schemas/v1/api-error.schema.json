{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "urn:commonroom:schema:v1:api-error",
  "title": "ApiError",
  "description": "Standard error response envelope for Commonroom product APIs.",
  "type": "object",
  "required": [
    "code",
    "message"
  ],
  "properties": {
    "code": {
      "type": "string",
      "minLength": 1,
      "maxLength": 64,
      "description": "Machine-readable error code identifier."
    },
    "message": {
      "type": "string",
      "minLength": 1,
      "maxLength": 500,
      "description": "Human-readable summary of the error."
    },
    "correlation_id": {
      "type": "string",
      "minLength": 1,
      "maxLength": 128,
      "description": "Opaque request correlation identifier for tracing across service boundaries."
    },
    "details": {
      "type": "object",
      "description": "Optional structured context for the error. Must not contain stack traces, credentials, or sensitive data."
    }
  },
  "additionalProperties": false
}

